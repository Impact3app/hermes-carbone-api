{
  "name": "hermes-carbone",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev -w apps/api-hermes",
    "start": "npm run start -w apps/api-hermes"
  }
}
